<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.yuzawa.shouta.techacademy.qa_app">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".MainActivity"
                android:label="@string/app_name"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".SettingActivity"></activity>
        <activity android:name=".QuestionSendActivity"
                  android:label="質問投稿画面"></activity>
        <activity android:name=".QuestionDetailActivity"
            android:label="質問詳細画面"></activity>
        <activity android:name=".AnswerSendActivity"
        android:label="回答投稿画面"></activity>
        <activity android:name=".FavoriteActivity"
                  android:label="お気に入り"></activity>
    </application>

</manifest>